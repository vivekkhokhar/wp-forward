# === WhatsApp session — THIS IS YOUR LOGIN. Never commit. ===
auth/
baileys/auth/

# === Secrets / keys / credentials ===
*.pem
*.key
*.p12
*.pfx
.env
.env.*
!*.example

# Server connection details (your instance IP / SSH access)
instance.env

# Personal config — contains phone numbers & group IDs (incl. third parties)
baileys/config.json

# Contacts export + derived name/face data — PII of third parties. Never commit.
*.vcf
*.csv
baileys/contacts.json
baileys/faces.json

# === Dependencies & build ===
node_modules/

# === Logs & process manager ===
*.log
forward.log
.pm2/

# === OS / editor junk ===
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
